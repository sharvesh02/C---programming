#include <stdio.h>
int main(){
	int a[10]={16, 18, 27,16, 23, 21, 19},i,count=0,n,k=0;
	for(n=0;i<=a[n];n++)
	{
	for(i=1;i<=a[n];i++)
	{
	if(a[n]%i==0)
	   {
	   	count++;
	   } 
    }
	if (count>2)
	{
	    k++;
	}
	count=0;
	i=1;
  }
	printf("the number of composite number is %d",k);
}
